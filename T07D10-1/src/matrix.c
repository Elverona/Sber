#include <stdio.h>
#include <stdlib.h>

// void show_matrix(int **matrix, int line, int cols) {
//     for (int i = 0; i < line; i++) {
//         for (int o = 0; o < cols; o++) {
//             printf("%d", matrix[i][o]);
//         }
//         printf("\n");
//     }
// }

// void free_matrix(int **matrix, int line) {
//     for (int i = 0; i < line; i++) {
//         free(matrix);
//     }
// }

int main() {
    // int matrix, line, cols;
    // char menu;
    printf(
        "Выберите метод:\n1. Статический\n2. Динамический (malloc)\n3. Динамический(calloc)\n4. "
        "Динамический(realloc)");

    // show_matrix(matrix, line, cols);
    // free_matrix(matrix, line);
    return 0;
}

// void show_matrix(int **matrix, int line, int cols) {
//     for (int i = 0; i < line; i++) {
//         for (int o = 0; o < cols; o++) {
//             printf("%d", matrix[i][o]);
//         }
//         printf("\n");
//     }
// }

// void free_matrix(int **matrix, int line) {
//     for (int i = 0; i < line; i++) {
//         free(matrix);
//     }
// }
